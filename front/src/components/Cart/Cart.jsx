import React from "react";
import './Cart.css'
import { observer } from "mobx-react-lite";

const Cart = observer(() => {
    return (
        <div className="home">
            <h1>Basket</h1>
        </div>
        
    );
  })

export default Cart